[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node ./dist/server/entry.mjs"

[variables]
HOST = "0.0.0.0"
NODE_TLS_REJECT_UNAUTHORIZED = "0"
